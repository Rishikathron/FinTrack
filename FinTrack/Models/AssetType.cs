namespace FinTrack.Models;

/// <summary>
/// Types of assets supported by the system.
/// Add new types here as the system grows (e.g., MutualFund, Stock, Crypto).
/// </summary>
public enum AssetType
{
    Gold,
    Silver,
    FD
}
